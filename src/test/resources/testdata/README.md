# Test Data

Place your Excel test data files here. Expected format:

- **TestData.xlsx** — Sheet name `Employee` with columns: `FirstName`, `LastName`, `Location`, `Username`, `Password`

These files are git-ignored by default (except this README).
